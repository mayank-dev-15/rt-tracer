import pytest
from click.testing import CliRunner
from rt_tracer.cli import main
def test_help():
    r = CliRunner().invoke(main, ['--help'])
    assert r.exit_code == 0
def test_status():
    r = CliRunner().invoke(main, ['status'])
    assert r.exit_code == 0
